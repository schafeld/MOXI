const paths = {
    exact: {
      // register singular page
      home: '/',
      login: '/login/',
      cart: '/cart/',
    },
    partial: {
      // register pages of a type
      anyPDP: '/p/',
      // anyPLP: '/c/',
      anyCheckout: '/checkout/',
    }
}

export default paths;